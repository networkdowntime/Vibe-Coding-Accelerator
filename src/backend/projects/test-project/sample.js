// Sample JavaScript file for testing LLM processing
function calculateSum(a, b) {
  return a + b;
}

function processArray(arr) {
  let result = [];
  for (let i = 0; i < arr.length; i++) {
    result.push(arr[i] * 2);
  }
  return result;
}

module.exports = { calculateSum, processArray };
